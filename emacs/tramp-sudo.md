# Emacs から SSH アカウントで sudo でファイル操作

* [Emacsでssh用アカウントでリモート入ってsudoしてファイル編集したい場合](https://qiita.com/sakadonohito/items/c8861cd99efafbdf5319)


`C-X-F` で

```
/ssh:username@server|sudo:server:/path/to/file
```
